---
// input: systemPageContent for editorial policy page context
// output: Static editorial policy page rendering from system config
// pos: src/pages/editorial-policy.astro (更新规则：文件变更需同步本注释与所属目录 README)

import SiteLayout from "../layouts/SiteLayout.astro";
import { systemPageContent } from "../content/system-pages";

const page = systemPageContent.editorialPolicy;
---

<SiteLayout title={page.title} description={page.description}>
  <main class="content-page">
    <section class="prose-block">
      <p class="eyebrow">Editorial Policy</p>
      <h1>{page.title}</h1>
      {page.sections.map((section) => (
        <>
          <h2>{section.heading}</h2>
          <p>{section.body}</p>
        </>
      ))}
    </section>
  </main>
</SiteLayout>
